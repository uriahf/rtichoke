# Adding color to the lift metric

Adding color to the lift metric

## Usage

``` r
add_color_to_lift(performance_dat, metric, color)
```

## Arguments

- performance_dat:

  the original performance data

- metric:

  the required metric

- color:

  the required color
