# Adding color to the confusion metric

Adding color to the confusion metric

## Usage

``` r
add_color_to_confusion_metric(performance_dat, metric, color)
```

## Arguments

- performance_dat:

  original performance data

- metric:

  the required metric

- color:

  the required color
