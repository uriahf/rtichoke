# Add Zebra colors to gt table

Add Zebra colors to gt table

## Usage

``` r
add_zebra_colors_to_gt_table(performance_table, add_zebra_colors)
```

## Arguments

- performance_table:

  gt performance table

- add_zebra_colors:

  add zebra colors or keep table as it is (FALSE)
