# Check stratified_by input

Check stratified_by input

## Usage

``` r
check_stratified_by_input(stratified_by)
```

## Arguments

- stratified_by:

  stratified by probability threshold or ppcr

## Examples

``` r
if (FALSE) { # \dontrun{
check_curve_input("probability_threshold")
check_curve_input("ppcr")
} # }
```
