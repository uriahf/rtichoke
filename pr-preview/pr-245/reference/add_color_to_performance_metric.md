# Adding color to performance metric

Adding color to performance metric

## Usage

``` r
add_color_to_performance_metric(performance_dat, metric, color)
```

## Arguments

- performance_dat:

  the original performance data

- metric:

  the required metric

- color:

  the required color
